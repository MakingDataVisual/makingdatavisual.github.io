var container = document.querySelector('#view'),
    loader = vega.loader(),
    renderType = 'svg',
    runtime = null,
    spec = null,
    view = null,
    current = null;

function load(name) {
}
